{{- if .Values.ocelotConfigMap.enabled }}
{{- $filePath := .Values.ocelotConfigMap.ocelotFile | default "files/default.json" }}
{{- if (.Files.Glob $filePath) }}
{{- $fileContent := .Files.Get $filePath }}
{{- $renderedTpl := tpl $fileContent . }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.ocelotConfigMap.name }}-ocelot-config
  namespace: {{ .Release.Namespace }}
data:
  {{ .Values.ocelotConfigMap.fileName | default "ocelot.json" }}: |-
{{ $renderedTpl | indent 4 }}
{{- else }}
{{- fail (printf "Config file not found: %s (relative to subchart root). Ensure files/ exists in microservice-base/." $filePath) }}
{{- end }}
{{- end }}
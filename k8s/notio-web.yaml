apiVersion: apps/v1
kind: Deployment
metadata:
  name: notio-web
  namespace: notio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notio-web
  template:
    metadata:
      labels:
        app: notio-web
    spec:
      imagePullSecrets:
        - name: zot-regcred
      containers:
        - name: notio-web
          image: registry.notioedu.com/notio/notio-web:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: notio-web
  namespace: notio
spec:
  selector:
    app: notio-web
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notio-web
  namespace: notio
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "20m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "120"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - platform.notioedu.com
      secretName: notioedu-wildcard-tls
  rules:
    - host: platform.notioedu.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: apigw
                port:
                  number: 80
          - path: /hubs
            pathType: Prefix
            backend:
              service:
                name: apigw
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: notio-web
                port:
                  number: 80
